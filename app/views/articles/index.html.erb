<h1>All Articles</h1>

<ul id="articles">
	<% @articles.each do |f| %>
		<li>
			<%= link_to f.title, article_path(f), class: 'article_title' %>
		</li>
	<% end %>
</ul>

<%= link_to "Create a New Article", new_article_path, class: 'new_article' %>